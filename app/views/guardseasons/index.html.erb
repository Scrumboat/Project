<h1>Vartiokaudet</h1>

<table>
  <tr>
    <th>Nimi</th>
    <th>Guardturn</th>
    <th>Alku pvm</th>
    <th>Loppu pvm</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @guardseasons.each do |guardseason| %>
  <tr>
    <td><%= guardseason.nimi %></td>
    <td><%= guardseason.guardturn_id %></td>
    <td><%= guardseason.alku_pvm %></td>
    <td><%= guardseason.loppu_pvm %></td>
    <td><%= link_to 'Näytä', guardseason %></td>
    <td><%= link_to 'Muokkaa', edit_guardseason_path(guardseason) %></td>
    <td><%= link_to 'Poista', guardseason, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Luo uusi vartiokausi', new_guardseason_path %>
