<h1>Telakat</h1>

<table>
  <tr>
    <th>Nimi</th>
    <th>Pituus</th>
    <th>Leveys</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @dockyards.each do |dockyard| %>
  <tr>
    <td><%= dockyard.name %></td>
    <td><%= dockyard.length %></td>
    <td><%= dockyard.width %></td>
    <td><%= link_to 'Näytä', dockyard %></td>
    <td><%= link_to 'Muokkaa', edit_dockyard_path(dockyard) %></td>
    <td><%= link_to 'Poista', dockyard, method: :delete, data: { confirm: 'Oletko varma?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Uusi telakka', new_dockyard_path %>
