<%= simple_form_for(@dockyard) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, :label => 'Nimi' %>
    <%= f.input :length, :label => 'Pituus' %>
    <%= f.input :width, :label => 'Leveys' %>
  </div>

  <div class="form-actions">
    <%= f.submit @teksti %>
  </div>
<% end %>
